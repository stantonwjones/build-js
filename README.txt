A simple bash script which concatenates files and minifies them using the node plugin uglifier-js

REQUIRES:: node.js with uglifier-js

HOWTO: 

1. list the file paths in build.txt
2. run ./build.sh < output file >

the files will be concatenated, minified, and output to the specified file.
